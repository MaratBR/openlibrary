package templates

import "github.com/MaratBR/openlibrary/internal/i18n"
import "github.com/MaratBR/openlibrary/internal/olhttp"
import "github.com/MaratBR/openlibrary/web/frontend"
import "github.com/MaratBR/openlibrary/internal/olhttp/webcomponents"

templ bmLayout(title string) {
	@olhttp.Doc(olhttp.DocProps{
		Title:        title,
		Scripts:      []string{"/_/assets/common.js", "/_/assets/public.api.js", "/_/assets/alpinejs.js"},
		CSS:          []string{"/_/assets/common.css"},
		InstantClick: false,
		AppendToHead: templ.Join(
			frontend.InlineCSSAsset(ctx, "embed-assets", "fonts.css"),
		),
	}) {
		{{
			l := i18n.GetLocalizer(ctx)
		}}
		@i18nExtractKeysByPrefix(l, "common")
		@i18nExtractKeysByPrefix(l, "bookManager")
		// TODO optimize?
		@i18nExtractKeysByPrefix(l, "book")
		{ children... }
	}
}

// type bmSidebarItem struct {
// 	Icon string
// 	URL string
// 	Text string
// }

// templ bmSidebar(items []bmSidebarItem) {
// 	{{
// 		r := olhttp.GetRequest(ctx)
// 	}}

// 	<ul class="dashboard-sidebar-list">
// 		for _, item := range items {
// 			{{
// 				active := r.URL.Path == item.URL
// 			}}
// 			<li
// 				if active {
// 					class="dashboard-sidebar-item dashboard-sidebar-item--active"
// 				} else {
// 					class="dashboard-sidebar-item"
// 				}
// 				>
// 				<a href={ item.URL }>
// 					<div class="dashboard-sidebar-item__icon">
// 						<i class={ item.Icon }></i>
// 					</div>
// 					<div class="dashboard-sidebar-item__label">
// 						{ item.Text }
// 					</div>
// 				</a>
// 			</li>

// 		}
// 	</ul>
// }


templ BM_Home() {
	{{
		l := i18n.GetLocalizer(ctx)
	}}
	@bmLayout(l.T("bookManager.home")) {
		@webcomponents.Island2(webcomponents.Island2Props{
			Name: "bookmanager/BM",
		})
	}
}

